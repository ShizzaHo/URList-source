import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ru.shizzaho.urlist',
  appName: 'URList',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
